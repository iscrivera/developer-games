package mx.multivelopers.kafka;

import io.quarkus.test.junit.NativeImageTest;

@NativeImageTest
public class NativeCityResourceIT extends CityResourceTest {

    // Execute the same tests but in native mode.
}